services:
  frontend:
    build:
      context: .
      dockerfile: docker.dev/Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - deskmint_net

networks:
  deskmint_net:
    driver: bridge